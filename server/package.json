{
  "name": "ioriparo-api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migra": "wrangler d1 migrations apply ioriparo-db --remote",
    "migra:locale": "wrangler d1 migrations apply ioriparo-db --local",
    "tipi": "wrangler types",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260722.1",
    "typescript": "~6.0.2",
    "wrangler": "^4.28.0"
  }
}
